# Copy to .env and fill values for production deployment
ENVIRONMENT=production
JWT_SECRET=replace-this-with-a-strong-secret
# Optional: precomputed bcrypt password hash for initial admin (leave empty to create admin manually)
ADMIN_PASSWORD_HASH=
CORS_ALLOWED_ORIGINS=https://yourdomain.example
MAX_QUEUE_SIZE=500
